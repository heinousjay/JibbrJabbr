less script must be modified - the environment detection should be hardcoded to the rhino case.  it is
designed to find the rhino shell stuff, and we really only define the few pieces we need:

readFile = function to read a file by name, relative to appPath

need to fix use of the global "name" to be the supplied function instead

