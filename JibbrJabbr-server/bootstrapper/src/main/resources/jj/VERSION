${friendlyname}
${version}
${git.branch}
${git.commit.user.name}
${git.commit.user.email}
${git.commit.id}
${git.commit.id.describe}
${git.commit.time}
${git.build.user.name}
${git.build.user.email}
${git.build.time}
